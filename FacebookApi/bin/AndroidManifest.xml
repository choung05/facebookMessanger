<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.facebookapi"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />
	<uses-permission android:name = "android.permission.INTERNET"/>
	<uses-permission android:name = "android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.test.facebookmessanger.MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        com.test.facebookmessanger.ChatManagerdroid:name = ".ChatMancom.test.facebookmessanger.FriendListivity>
        <activity android:name = ".FriendList"></activity>
        <activity android:name = ".Main"></activity>
        <service android:name  = "com.test.facebookmessanger.ChatServiceListener"></service>
    </application>

</manifest>